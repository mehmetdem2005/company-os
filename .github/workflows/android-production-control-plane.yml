name: Android Production Control Plane Legacy

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Use V2 validation
        run: echo "Canonical validation moved to android-production-control-plane-v2.yml"
