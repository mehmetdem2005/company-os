# Building Company OS Android

The Android application is a React Native app. The user interface is written in TypeScript/React Native; Android lifecycle, TurboModule and Godot runtime boundaries are written in Kotlin.

The installable APK is built by `.github/workflows/android-apk.yml` after the mobile source archive is updated.
